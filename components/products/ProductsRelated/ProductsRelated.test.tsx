import { render } from '@testing-library/react';
import ProductsRelated from './ProductsRelated';

describe('ProductsRelated', () => {
  it('renders', () => {
    render(<ProductsRelated />);
  });
});
